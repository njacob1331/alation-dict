[project]
name = "alation-dict"
version = "0.1.0"
description = "Tools for managing Alation dictionary records"
readme = "README.md"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
